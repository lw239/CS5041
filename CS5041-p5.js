//var barLines = ??
//Note to Lauren: Bars have 5 lines


function setup() {
    createCanvas(660, 360);
    // loadImage('bars.png', img => {
    //     image(img, 0, 0);
}


// I don't know if I have to keep everyting in here but it looks like 
// you can only draw with this one function 
function draw() {
    line(40, 50, 500, 50);
    line(40, 100, 500, 100);
    line(40, 150, 500, 150);
    line(40, 200, 500, 200);
    line(40, 250, 500, 250);
}

// //I guess "draw" is the only function name that can be used...
// function draw(){

// }

function keyPressed() {

}